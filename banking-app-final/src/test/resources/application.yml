spring:
  main:
    banner-mode: off
  jpa:
    defer-datasource-initialization: true
    properties:
      generate_statistics: true
    hibernate.ddl-auto: create-drop
  sql:
    init:
      platform: h2
